version: v1.0
name: Initial Pipeline
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804
blocks:
  - name: Build
    task:
      jobs:
        - name: Build
          commands:
            - checkout
            - npm install
            - npm run build
      prologue:
        commands:
          - nvm install --lts erbium
          - sem-version node --lts carbon
